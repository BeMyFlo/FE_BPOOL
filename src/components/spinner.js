// Spinner.js
import React from 'react';
import '../css/spinner.css'; // Import file CSS cho spinner

const Spinner = () => {
  return (
    <div className="spinner"></div>
  );
};

export default Spinner;
